<Window x:Class="VisionFramework.UI.Views.SystemInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="系统信息" Height="420" Width="440"
        WindowStartupLocation="CenterOwner"
        Background="#FF1E1E2E" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="VisionFramework" FontSize="24" FontWeight="Bold"
                   Foreground="#7B68EE" HorizontalAlignment="Center" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="机器视觉检测系统" FontSize="13" Foreground="#888"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="2" Background="#FF2D2D3F" CornerRadius="4" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="32"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="系统版本：" Foreground="#888" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TblVersion" Text="1.0.0" Foreground="#CCC" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="设计者：" Foreground="#888" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TblDesigner" Text="姜泽凯" Foreground="#CCC" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="构建日期：" Foreground="#888" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TblBuildDate" Text="2026-08-16" Foreground="#CCC" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="运行环境：" Foreground="#888" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TblRuntime" Text=".NET Framework 4.8" Foreground="#CCC" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- VisionPro 路径配置 -->
        <GroupBox Grid.Row="3" Header="VisionPro 路径" Foreground="#AAA" Margin="0,12,0,0">
            <Grid Margin="8,8,8,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TxtVisionProPath" Height="28" FontSize="11"
                         Background="#FF2D2D3F" Foreground="White" BorderBrush="#444" Padding="6,2"
                         VerticalContentAlignment="Center"/>
                <Button Grid.Column="1" Content="浏览" Height="28" Margin="6,0,0,0"
                        Background="#FF7B68EE" Foreground="White" BorderThickness="0" Cursor="Hand"
                        FontSize="12" Click="BtnBrowseVp_Click"/>
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="5" Text="© 2026 姜泽凯. All rights reserved."
                   Foreground="#555" FontSize="11" HorizontalAlignment="Center" Margin="0,16,0,0"/>
    </Grid>
</Window>
